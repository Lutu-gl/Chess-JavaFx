package Pieces;

import javafx.scene.control.Label;
import javafx.scene.image.ImageView;
import Chess.Color;

public class Rook extends Piece {
    public Rook(ImageView img, Label l, Color color, String name) {
        super(img, l, color, name);
    }
}
