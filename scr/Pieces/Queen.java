package Pieces;

import javafx.scene.control.Label;
import javafx.scene.image.ImageView;
import Chess.Color;

public class Queen extends Piece{
    public Queen(ImageView img, Label l, Color color, String name) {
        super(img, l, color, name);
    }
}
